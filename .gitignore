# Dependencies
node_modules/
.next/
out/

# Python
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.venv/
venv/
*.egg-info/

# IDE / OS
.DS_Store
*.swp
*.swo
*~

# Env
.env
.env.local
.env*.local
deploy.env

# Build / logs
*.log
npm-debug.log*

# Fichiers de test temporaires
test
.test.swp

# Cahier des charges interne + suivi de projet (non destinés à la publication GitHub)
cahierDesCharges/
PLAN.md
idea/
backEnd/scripts/
backEnd/sql/
productivite/

# Cache de build TypeScript (régénéré automatiquement)
*.tsbuildinfo


# Réglages Claude Code personnels (permissions locales)
.claude/settings.local.json

# Sauvegardes de la base (backup/backupDb.sh). JAMAIS dans le dépôt : il est
# public et un dump contient l'intégralité des données clients.
#
# Le contenu de backup/data est ignoré mais le répertoire est conservé, via un
# .gitkeep explicitement excepté : les scripts s'attendent à le trouver.
backup/data/*
!backup/data/.gitkeep
backups/
*.sql.gz
*.sql.bz2
